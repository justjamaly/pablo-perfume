<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>Pablo Perfumes | Luxury Fragrances</title>
<meta name="description" content="Luxury designer perfumes with same-day delivery in Dar es Salaam.">

<link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;500;600&display=swap" rel="stylesheet">

<style>
:root{
  --bg:#f6f6f6; --card:#fff; --text:#111; --muted:#666;
  --gold:#c9a24d; --border:#e5e5e5;
}
*{box-sizing:border-box}
body{margin:0;font-family:Poppins;background:var(--bg);color:var(--text)}

nav{
  background:#fff;border-bottom:1px solid var(--border);
  padding:15px 25px;display:flex;justify-content:space-between;align-items:center;
  position:sticky;top:0;z-index:100
}
nav strong{letter-spacing:2px}
nav a{text-decoration:none;color:#000;margin-left:20px;font-size:.9em}

header{
  background:linear-gradient(to right,#000,#1c1c1c);
  color:#fff;text-align:center;padding:110px 20px
}
header h1{font-size:3em;letter-spacing:4px}
header span{color:var(--gold)}

section{max-width:1300px;margin:auto;padding:70px 20px}
h2{text-align:center;margin-bottom:30px}

.controls{text-align:center;margin-bottom:35px}
.controls input,.controls select{
  padding:10px;width:220px;border:1px solid var(--border);margin:6px
}
.controls button{
  padding:8px 16px;border:1px solid var(--border);background:#fff;cursor:pointer;margin:4px
}

.grid{
  display:grid;
  grid-template-columns:repeat(auto-fit,minmax(240px,1fr));
  gap:30px
}

.card{
  background:var(--card);
  border:1px solid var(--border);
  border-radius:14px;
  padding:20px;
  text-align:center;
  transition:.3s;
  position:relative
}
.card:hover{
  transform:translateY(-6px);
  box-shadow:0 12px 30px rgba(0,0,0,.12)
}
.card img{
  width:100%;
  height:220px;
  object-fit:contain
}
.card h3{font-size:1em;margin:12px 0 6px}
.desc{font-size:.8em;color:var(--muted)}

.price{
  color:var(--gold);
  font-weight:600;
  margin-top:6px
}
.old{
  text-decoration:line-through;
  color:#999;
  font-size:.8em;
  margin-right:6px
}

.badge{
  position:absolute;
  top:12px;left:12px;
  background:var(--gold);
  padding:4px 8px;
  font-size:.7em;
  font-weight:600;
  border-radius:4px
}

.card button{
  background:#000;color:#fff;
  padding:10px 18px;
  border-radius:4px;
  border:none;
  margin-top:10px;
  cursor:pointer
}

#cartBtn{
  position:fixed;
  bottom:20px;right:20px;
  background:#000;color:#fff;
  padding:14px 22px;
  border-radius:50px;
  cursor:pointer
}

#cart{
  position:fixed;
  right:-420px;
  top:0;height:100%;
  width:400px;
  background:#fff;
  box-shadow:-6px 0 30px rgba(0,0,0,.15);
  padding:20px;
  transition:.3s;
  z-index:200
}

#cart h3{margin-top:0}
.cart-item{margin-bottom:12px}
.cart-item small{color:#666}

footer{
  background:#111;
  color:#bbb;
  text-align:center;
  padding:30px;
  font-size:.85em
}

@media(max-width:600px){
  header h1{font-size:2.2em}
  #cart{width:100%}
}
</style>
</head>
<body>

<nav>
<strong>PABLO PERFUMES</strong>
<div>
<a href="#shop">Shop</a>
<a href="#contact">Contact</a>
</div>
</nav>

<header>
<h1>PABLO <span>PERFUMES</span></h1>
<p>Luxury Fragrances • Same-Day Delivery</p>
</header>

<section id="shop">
<h2>Shop Fragrances</h2>

<div class="controls">
<input id="search" placeholder="Search perfume...">
<select id="sort">
<option value="default">Sort by price</option>
<option value="low">Low → High</option>
<option value="high">High → Low</option>
</select>
</div>

<div class="grid" id="products"></div>
</section>

<footer>
© 2026 Pablo Perfumes • Luxury You Can Smell
</footer>

<div id="cartBtn">🛒 Cart</div>

<div id="cart">
<h3>Cart</h3>
<div id="cartItems"></div>
<strong>Total: TZS <span id="total">0</span></strong>
<br><br>
<button onclick="checkout()">Order via WhatsApp</button>
</div>

<script>
const products=[
 {name:"Dior Sauvage",price:85000,category:"men",img:"https://images.unsplash.com/photo-1523293182086-7651a899d37f"},
 {name:"Bleu de Chanel",price:90000,discount:80000,category:"men",img:"https://images.unsplash.com/photo-1458538977777-0549b2370168"},
 {name:"Oud Wood",price:120000,category:"unisex",img:"https://images.unsplash.com/photo-1615160460366-2c9a41771b51"},
 {name:"Coco Mademoiselle",price:95000,discount:85000,category:"women",img:"https://images.unsplash.com/photo-1526045431048-7c2e0c3f1c1d"}
];

let cart=[];

const grid=document.getElementById("products");

function render(){
 grid.innerHTML="";
 products.forEach((p,i)=>{
  grid.innerHTML+=`
  <div class="card">
   ${p.discount?'<div class="badge">SALE</div>':''}
   <img src="${p.img}">
   <h3>${p.name}</h3>
   <div class="desc">${p.category.toUpperCase()}</div>
   <div class="price">
    ${p.discount?`<span class="old">TZS ${p.price}</span> TZS ${p.discount}`:`TZS ${p.price}`}
   </div>
   <button onclick="add(${i})">Add to Cart</button>
  </div>`;
 });
}
render();

function add(i){
 cart.push(products[i]);
 updateCart();
 document.getElementById("cart").style.right="0";
}

function updateCart(){
 let total=0;
 document.getElementById("cartItems").innerHTML="";
 cart.forEach(p=>{
  total+=p.discount||p.price;
  document.getElementById("cartItems").innerHTML+=`
   <div class="cart-item">
    ${p.name}<br><small>TZS ${p.discount||p.price}</small>
   </div>`;
 });
 document.getElementById("total").textContent=total;
}

document.getElementById("cartBtn").onclick=()=>{
 document.getElementById("cart").style.right="0";
};

function checkout(){
 let msg="Hello Pablo Perfumes,%0AI want to order:%0A";
 let total=0;
 cart.forEach(p=>{
  msg+=`- ${p.name}%0A`;
  total+=p.discount||p.price;
 });
 msg+=`Total: TZS ${total}`;
 window.open("https://wa.me/255675999999?text="+msg,"_blank");
}
</script>

</body>
</html>

